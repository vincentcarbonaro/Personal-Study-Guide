// Write a method, digitalRoot(num).
// It should sum the digits of a positive integer.
// If it is greater than or equal to 10, sum the digits of the resulting number.
// Keep repeating until there is only one digit in the result, called the "digital root".
// Do not use string conversion within your method.

function digitalRoot(num) {
}

console.log();
console.log( digitalRoot(0) == 0 );
console.log( digitalRoot(5) == 5 );
console.log( digitalRoot(10) == 1 );
console.log( digitalRoot(65536) == 7 );
console.log( digitalRoot(123456789) == 9 );
console.log();
